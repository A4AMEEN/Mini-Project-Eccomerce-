<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details -</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Your custom CSS -->
    <link rel="stylesheet" href="/css/style.css"> <!-- Update the path as needed -->
</head>
<style>
    .product-name {
    font-size: 24px;
    font-weight: bold;
    color: #333; /* Update the color as needed */
}

/* Example styling for the product price */
.product-price {
    font-size: 20px;
    color: #4CAF50; /* Update the color as needed */
}

</style>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"></a>
    <!-- Add your navigation links if needed -->
</nav>
<a href="orders" class="btn btn-primary">Back to Orders</a>
 

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 col-lg-6">
            <div class="product-images">
                <div id="product-carousel" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        <% product.images.forEach(function(image, index){ %>
                            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                <img src="/images/<%= image %>" class="d-block w-100" alt="Product Image <%= index + 1 %>">
                            </div>
                        <% }); %>
                    </div>
                    <a class="carousel-control-prev" href="#product-carousel" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#product-carousel" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" ariahidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="product-details">
                <h2 class="product-name">ITEM:- <%= product.name %></h2>
                <p class="price">
                    <h2 class="product-price"><span>PRICE:-<%= product.price %>&#8377;</span></h2>
                    <h3>Brand:-<%= product.brand %></h3>
                    <!-- Add more product details here -->
                    <h5>Nike, a global leader in athletic footwear, offers a diverse range of shoes known for innovation, performance, and style. From iconic designs like the Air Max and Air Jordan to cutting-edge running and training shoes, Nike caters to athletes and fashion enthusiasts alike. With a commitment to quality and technology, Nike shoes blend comfort and functionality, embodying the brand's ethos of empowering individuals to push their limits in both sports and fashion.</h5>
                </p>
                <!-- Add other product details as needed -->
            </div>
        </div>
    </div>
</div>
<script>
    $(document).ready(function () {
        let MainImg = document.getElementById('zoom');
        let smalImg = document.getElementsByClassName('smalImg');

        smalImg[0].onclick = () => {
            MainImg.src = smalImg[0].src;
            initializeZoom();
        }
        smalImg[1].onclick = () => {
            MainImg.src = smalImg[1].src;
            initializeZoom();
        }
        smalImg[2].onclick = () => {
            MainImg.src = smalImg[2].src;
            initializeZoom();
        }
        smalImg[3].onclick = () => {
            MainImg.src = smalImg[3].src;
            initializeZoom();
        }
        smalImg[4].onclick = () => {
            MainImg.src = smalImg[4].src;
            initializeZoom();
        }

        // Initial zoom for the first image
        initializeZoom();

        function initializeZoom() {
            $("#zoom").imagezoomsl({
                zoomrange: [1, 1],
                zoomstart: 1,
                zoomstartwidth: 10,
                zoomstartheight: 40,
                innerzoom: true,
                zoomcontainer: $('#product-carousel')
            });
        }
    });
</script>

<!-- Additional styling or scripts can be added here -->

</body>
</html>
